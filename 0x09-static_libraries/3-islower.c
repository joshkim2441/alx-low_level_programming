#include "main.h"

/**
 * _islower - function prints 1 or 0
 * depending on the input
 * @c: the first parameter
 *
 * Description: it prints all lowercase letters
 * Return: Always(0) success
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
